terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 4.0"
    }
  }
}

provider "aws" {
  region = terraform.workspace == "default" ? var.primary_region : var.secondary_region
}